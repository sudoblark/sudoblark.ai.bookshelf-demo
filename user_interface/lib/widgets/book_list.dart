/*
Copilot: This file implements a scrollable list container for displaying multiple books.

Goals:
1. Accept a List<Book> and display each using book_card.dart.
2. Provide a scrollable ListView with proper spacing.
3. Show a message if the list is empty.
4. Handle null or empty list gracefully.

Expectations:
- Use ListView.builder for efficient rendering.
- Add padding and spacing between cards.
- Use Constants for spacing values.
- Keep the widget simple and composable.

Do not include:
- Data fetching (parent screen's responsibility).
- State management.
- API calls.
- Loading indicators (parent screen handles that).
*/