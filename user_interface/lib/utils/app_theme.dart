/*
Copilot: This file defines the Material Design theme for the app.

Goals:
1. Define a cohesive color scheme (primary, secondary, accent colors).
2. Set typography (font families, sizes for headlines, body, etc.).
3. Create a consistent theme that's used throughout the app.
4. Define spacing and sizing constants (padding, border radius, etc.).
5. Support light mode (dark mode optional for future).

Expectations:
- Use ThemeData from Flutter Material.
- Define colors that work for a local ETL/data app (professional, clean).
- Include clear separation between different text styles.
- Use consistent spacing throughout.
- Export a buildTheme() function or appTheme constant.

Do not include:
- Business logic.
- Widget code.
- API configuration.
*/