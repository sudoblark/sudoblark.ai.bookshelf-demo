# IAM roles are now owned by their respective infrastructure modules:
# - Lambda IAM roles   → module "lambda"  (modules/infrastructure/lambda)
# - Glue crawler roles → module "glue"    (modules/infrastructure/glue)
