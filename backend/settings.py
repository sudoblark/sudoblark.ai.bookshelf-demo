
# Copilot: Centralised configuration for the Flask backend.
#
# Provide:
# - RAW_DIR and PROCESSED_DIR as absolute paths (computed from this file’s location).
# - ALLOWED_EXTENSIONS set for image uploads.
# - DEFAULT_HOST and DEFAULT_PORT for Flask.
# - MAX_CONTENT_LENGTH (optional) to limit upload size.
#
# Constraints:
# - Keep configuration simple and readable.
# - Allow environment variable overrides where helpful (optional).
# - Do not include processor-specific logic here.
